📌 Project Idea: Blog/Articles API

This is realistic and covers most real-world needs:

Users can sign up / login

Users can create posts

Users can comment on posts

Optional: Roles (admin, user) → restrict actions

1. Users Table

Stores registered users.
We’ll use UUID for id and store hashed password.

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

CREATE TABLE IF NOT EXISTS users (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    name VARCHAR(100) NOT NULL,
    email VARCHAR(150) UNIQUE NOT NULL,
    password VARCHAR(200) NOT NULL,
    role VARCHAR(20) DEFAULT 'user' CHECK (role IN ('user', 'admin')),
    verified BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

2. Posts Table

Each post belongs to a user (foreign key).

CREATE TABLE IF NOT EXISTS posts (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    title VARCHAR(200) NOT NULL,
    content TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

3. Comments Table

Each comment belongs to a post and a user.

CREATE TABLE IF NOT EXISTS comments (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    post_id UUID NOT NULL REFERENCES posts(id) ON DELETE CASCADE,
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    content TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

4. Password Reset Tokens (optional but 🔑 for forgot/reset password)

When a user requests password reset → store token here.

CREATE TABLE IF NOT EXISTS password_resets (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    token VARCHAR(200) NOT NULL,
    expires_at TIMESTAMP NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


✅ Now you have:

users → authentication + roles

posts → content created by users

comments → interactions between users and posts

password_resets → security feature

⚡ Example Query Workflows

Signup → Insert into users

Login → Select email, compare password

Create Post → Insert into posts with user_id

Get All Posts → Join with users (to show author name)

Add Comment → Insert into comments with user_id & post_id

Forgot Password → Insert token into password_resets

👉 This schema is scalable and lets you practice:

Foreign keys ✅

Cascade delete (if user is deleted, posts/comments go too) ✅

Roles & permissions ✅

Auth workflows ✅

Pagination/sorting/filtering (easy on posts & comments) ✅